# bluestock-week2-data-analytics
This Jupyter Notebook demonstrates how to extract data from a public REST API using Python. It sends a GET request to the JSONPlaceholder API, retrieves JSON data, converts it into a structured Pandas DataFrame, and exports the data into a CSV file for further analysis.
